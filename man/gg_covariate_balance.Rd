% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gg_covariate_balance.R
\name{gg_covariate_balance}
\alias{gg_covariate_balance}
\alias{autoplot.ppm_cov_tidy}
\title{Create a ggplot2 Covariate Balance Plot}
\usage{
gg_covariate_balance(
  data,
  dv_color = "black",
  cov_color = "grey70",
  dv_linetype = "solid",
  cov_linetypes = NULL,
  hline = 0,
  ylim = c(-2, 2),
  xlab = "Time",
  ylab = "SD",
  title = NULL,
  subtitle = NULL,
  show_legend = FALSE,
  strip_text_y_size = 8.5,
  theme_fn = ggplot2::theme_bw
)

\method{autoplot}{ppm_cov_tidy}(object, ...)
}
\arguments{
\item{data}{A \code{ppm_cov_tidy} data frame from
\code{\link{pretty_covariate_balance}}.}

\item{dv_color}{Color for DV lines. Default \code{"black"}.}

\item{cov_color}{Color for covariate lines. Default \code{"grey70"}.}

\item{dv_linetype}{Linetype for DV lines. Default \code{"solid"}.}

\item{cov_linetypes}{Character vector of linetypes for covariates. If
\code{NULL} (default), cycles through \code{"dashed"}, \code{"twodash"},
\code{"dotted"}, \code{"dotdash"}, \code{"longdash"}.}

\item{hline}{Y-intercept for reference line. Default 0. Set to \code{NULL}
to remove.}

\item{ylim}{Y-axis limits as a length-2 numeric vector. Default
\code{c(-2, 2)}. Set to \code{NULL} for automatic limits.}

\item{xlab}{X-axis label. Default \code{"Time"}.}

\item{ylab}{Y-axis label. Default \code{"SD"}.}

\item{title}{Optional plot title.}

\item{subtitle}{Optional plot subtitle.}

\item{show_legend}{Logical. Show legend? Default \code{FALSE}.}

\item{strip_text_y_size}{Font size for row strip labels. Default 8.5.
Set to \code{NULL} to use the theme default.}

\item{theme_fn}{A ggplot2 theme function. Default \code{theme_bw}.}

\item{object}{A \code{ppm_cov_tidy} data frame.}

\item{...}{Additional arguments passed to \code{gg_covariate_balance}.}
}
\value{
A \code{ggplot} object that can be further customized with
standard ggplot2 syntax.
}
\description{
Produces a faceted covariate balance plot showing standardized mean
differences across matching stages. Dependent variables are drawn as
black solid lines; covariates as grey lines with distinct linetypes.
}
\details{
The default layout uses \code{facet_grid(model ~ stage)}, where models
(different DVs / subsamples) form the rows and matching stages form the
columns, reproducing the standard PanelMatch covariate-balance diagnostic.
}
\examples{
\dontrun{
cov_data <- pretty_covariate_balance(
  "Cong-FIN; US finan" = list(cov1, cov2, cov3),
  "Cong-BAN; US finan" = list(cov4, cov5, cov6),
  dv = c("congress_fin", "congress_ban")
)
gg_covariate_balance(cov_data)

# With legend and custom colors
gg_covariate_balance(cov_data, show_legend = TRUE, cov_color = "grey50")

# Add a vertical line at the last pre-treatment period
gg_covariate_balance(cov_data) +
  geom_vline(xintercept = 3, lty = "dashed")
}

}
\seealso{
\code{\link{pretty_covariate_balance}} to prepare the input data,
\code{\link{ggplot_panel_estimate}} for treatment effect plots,
\code{\link{gg_placebo_test}} for placebo test plots.
}
