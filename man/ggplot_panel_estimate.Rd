% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggplot_panel_estimate.R
\name{ggplot_panel_estimate}
\alias{ggplot_panel_estimate}
\alias{autoplot.ppm_tidy}
\title{Create a ggplot2 Coefficient Plot from PanelEstimate Results}
\usage{
ggplot_panel_estimate(
  data,
  dodge_width = 0.5,
  point_size = 2.2,
  errorbar_alpha = 0.5,
  errorbar_width = 0,
  shapes = NULL,
  show_signif_shapes = TRUE,
  legend_labels = NULL,
  footnote = "Filled markers denote statistical significance (CI excludes zero).",
  footnote_size = NULL,
  xlab = "Time",
  ylab = "ATT",
  title = NULL,
  subtitle = NULL,
  hline = 0,
  facet_by = NULL,
  theme_fn = ggplot2::theme_minimal
)

\method{autoplot}{ppm_tidy}(object, ...)
}
\arguments{
\item{data}{A \code{ppm_tidy} data frame from \code{\link{tidy_panel_estimate}}.}

\item{dodge_width}{Width of position dodge for multiple models. Default 0.5.}

\item{point_size}{Size of point estimates. Default 2.2.}

\item{errorbar_alpha}{Alpha transparency for error bars. Default 0.5.}

\item{errorbar_width}{Width of error bar caps. Default 0 (no caps).}

\item{shapes}{A character vector of hollow shape names, one per model.
Filled counterparts are paired automatically. If \code{NULL} (default),
shapes cycle through: \code{"circle"}, \code{"diamond"}, \code{"triangle"},
\code{"square"}, \code{"triangle_down"}.
Available names: \code{"circle"}, \code{"square"}, \code{"triangle"},
\code{"diamond"}, \code{"triangle_down"}. Numeric codes (0-14) are also
accepted for advanced users.}

\item{show_signif_shapes}{Logical. If \code{TRUE} (default), uses different
shapes for significant vs. non-significant estimates. If \code{FALSE},
uses uniform shapes per model.}

\item{legend_labels}{Optional character vector to override legend labels
(one per model, in input order).}

\item{footnote}{Character string for the significance footnote. Set to
\code{NULL} to suppress. Default explains hollow vs. filled convention.}

\item{footnote_size}{Font size for the footnote. Default \code{NULL},
which matches the axis title size from the active theme.}

\item{xlab}{X-axis label. Default \code{"Time"}.}

\item{ylab}{Y-axis label. Default \code{"ATT"}.}

\item{title}{Optional plot title.}

\item{subtitle}{Optional plot subtitle.}

\item{hline}{Intercept for reference line. Default 0. Set to \code{NULL}
to remove.}

\item{facet_by}{Optional variable name to facet by (e.g., \code{"label"}).
Default \code{NULL} (no faceting).}

\item{theme_fn}{A ggplot2 theme function. Default \code{theme_minimal}.}

\item{object}{A \code{ppm_tidy} data frame.}

\item{...}{Additional arguments passed to \code{ggplot_panel_estimate}.}
}
\value{
A \code{ggplot} object that can be further customized with
standard ggplot2 syntax.
}
\description{
Produces a customizable ggplot2 coefficient plot showing point estimates
and confidence intervals across lead periods. Supports multiple models
with dodged positions and significance-based shape coding (hollow = not
significant, filled = significant). A footnote is added by default.
}
\details{
The returned object is a standard \code{ggplot} object, so you can add
any ggplot2 layers, scales, or themes on top of it.
}
\examples{
\dontrun{
# Single model
tidy_panel_estimate(summary(pe1), labels = "My Model") |>
  ggplot_panel_estimate()

# Multiple models with custom hollow shapes
combined <- tidy_panel_estimate(
  "Energy Dept." = summary(pe1[[1]]),
  "State Dept."  = summary(pe2[[1]]),
  "Congress"     = summary(pe3[[1]]),
  "EOP"          = summary(pe4[[1]])
)
ggplot_panel_estimate(combined, shapes = c("circle", "diamond", "triangle", "square"))

# Suppress footnote
ggplot_panel_estimate(combined, footnote = NULL)
}

}
\seealso{
\code{\link{tidy_panel_estimate}} to prepare the input data,
\code{\link{gg_placebo_test}} for placebo test plots,
\code{\link{gg_covariate_balance}} for covariate balance plots.
}
