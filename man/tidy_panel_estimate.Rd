% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy_panel_estimate.R
\name{tidy_panel_estimate}
\alias{tidy_panel_estimate}
\title{Tidy PanelEstimate Summaries into a Data Frame}
\usage{
tidy_panel_estimate(..., labels = NULL)
}
\arguments{
\item{...}{One or more PanelEstimate summary objects (or raw PanelEstimate
objects, which will be summarized automatically). Can also be a single
named list of summaries.}

\item{labels}{A character vector of labels, one per summary. Used to
distinguish models in the plot legend. If \code{NULL} and inputs are
named (either as named arguments or a named list), the names are used.
Otherwise defaults to \code{"Model 1"}, \code{"Model 2"}, etc.}
}
\value{
A data frame (with class \code{"ppm_tidy"}) containing columns:
\describe{
\item{term}{Lead period label (e.g., "t+0", "t+1", ...)}
\item{estimate}{Point estimate (ATT)}
\item{std.error}{Standard error}
\item{conf.low}{Lower confidence bound}
\item{conf.high}{Upper confidence bound}
\item{label}{Model label (ordered factor preserving input order)}
\item{signif}{Whether the CI excludes zero ("Signif" or "Non-signif")}
}
}
\description{
Converts one or more \code{summary(PanelEstimate(...))} objects into a
single tidy data frame suitable for ggplot2 plotting.
}
\examples{
\dontrun{
# Single model
tidy_panel_estimate(summary(pe), labels = "My Model")

# Multiple models
tidy_panel_estimate(
  summary(pe.results_e1[[1]]),
  summary(pe.results_e2[[1]]),
  summary(pe.results_e3[[1]]),
  labels = c("Energy Dept.", "State Dept.", "Congress")
)

# Named arguments (names become labels automatically)
tidy_panel_estimate(
  "Energy Dept." = summary(pe.results_e1[[1]]),
  "State Dept."  = summary(pe.results_e2[[1]])
)
}

}
\seealso{
\code{\link{ggplot_panel_estimate}} to plot the result,
\code{\link{pretty_placebo_test}} for placebo test results,
\code{\link{pretty_covariate_balance}} for covariate balance.
}
